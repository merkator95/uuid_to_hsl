## 1.0.0

- Rewrite `UuidToHsl` with hash-based HSL generation and configurable ranges.
- Add new instance API (`colorFromUuid`, `hslColorFromUuid`, hue/sat/light getters).
- Add Flutter example app with grid view and PNG/CSV export.
- Update SDK/Flutter constraints.
- Mark old static methods as deprecated.

## 0.0.1

* Initial release
